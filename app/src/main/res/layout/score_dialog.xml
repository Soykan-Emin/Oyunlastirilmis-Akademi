<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/round_corner_buttom"
    android:gravity="center"
    android:padding="24dp">

    <TextView
        android:id="@+id/score_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="Tebrikler!"
        android:textColor="@color/white"
        android:gravity="center"
        android:textSize="22sp"
        android:textStyle="bold"
        android:layout_marginBottom="16dp" />

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp">

        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/score_progress_indicator"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            app:indicatorColor="@color/cyan_accent"
            app:trackColor="@color/glass_white_border"
            tools:progress="60"
            app:trackCornerRadius="20dp"
            app:trackThickness="10dp"
            app:indicatorSize="100dp" />

        <TextView
            android:id="@+id/score_progress_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="50%"
            android:textColor="@color/gold_accent"
            android:textSize="20sp"
            android:layout_centerInParent="true"
            android:textStyle="bold" />
    </RelativeLayout>

    <TextView
        android:id="@+id/score_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="10 sorudan 6 doğru yaptınız"
        android:textColor="@color/text_secondary"
        android:gravity="center"
        android:textSize="14sp"
        android:layout_marginBottom="24dp" />

    <Button
        android:id="@+id/finish_btn"
        android:layout_width="match_parent"
        android:layout_height="54dp"
        android:background="@drawable/btn_cyan_accent"
        android:text="Tamamla"
        android:textColor="@color/deep_purple"
        android:textStyle="bold"
        android:textSize="16sp" />

</LinearLayout>